version: 0.2

phases:
  pre_build:
    commands:
      - git checkout angular
      - sudo apt-get update -y && sudo apt-get install -y node
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - dist/**/*
